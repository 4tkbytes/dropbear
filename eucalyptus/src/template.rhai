/// A function in the eucalyptus editor that runs the function once 
/// during the spawning of that item.
fn init() { }

/// A function that loops every frame, allowing you to change
/// any properties like position or states.  
fn update() { }