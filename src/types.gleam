//// Different types and values available to be used.

/// A generic value. It is used in the entity properties.
pub type Value {
    /// Integer
    Int(Int)
    /// Float
    Float(Float)
    /// String
    String(String)
    /// Boolean
    Bool(Bool)
}